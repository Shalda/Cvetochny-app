import { ToogleSubmenuDirective } from './toogle-submenu.directive';

describe('ToogleSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
