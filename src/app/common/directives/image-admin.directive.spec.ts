import { ImageAdminDirective } from './image-admin.directive';

describe('ImageAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageAdminDirective();
    expect(directive).toBeTruthy();
  });
});
